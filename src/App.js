import { Route, Router, Switch, Redirect} from 'react-router-dom'

function App() {
  return (
    <div>
      <Switch>
      <Router>
        <Route> </Route>

      </Router>
      <Redirect to='/'/>
      </Switch>
    </div>
  );
}

export default App;
